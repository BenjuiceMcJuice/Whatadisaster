# Dev Log — 2026-07-22

## LinkedIn launch — usage monitoring session

App link posted on LinkedIn by Heather (stakeholder). Session focus: confirm site-usage monitoring is working and interpret first-day traffic.

### Analytics review (no code change)
- **Cloudflare Traffic tab** (zone, bots included): ~30 unique visitors in first 24h.
- **Cloudflare Web Analytics**: two site entries exist and this caused confusion —
  - `whatadisaster.pages.dev +1` (JS Snippet, "+1" = also covers `whatadisaster.uk`) — **the working one**, 14 human visits (bots excluded).
  - `whatadisaster.uk` (Automatic setup) — a **redundant duplicate**, showing 0; its beacon never fired. Safe to delete later; ignored for now.
- URL breakdown (bots excluded) confirmed **nearly all traffic is on `whatadisaster.uk/`**, only ~1 hit on `whatadisaster.pages.dev/` (likely self-testing). Heather's post uses the correct `.uk` URL.
- Core Web Vitals healthy: **LCP P75 = 1,594ms (Good, <2.5s)**; median 1,134ms. Slower P90/P99 (2.9s/3.6s) is the expected slow-connection tail.

### Change: scoped pages.dev → whatadisaster.uk redirect
Cheap insurance so anyone who reaches the old `whatadisaster.pages.dev` URL (reshares, bookmarks, copied links) is forwarded to the canonical domain instead of hitting the "You're on a Dev Build" modal.

- Redirects **only the exact bare production alias** `whatadisaster.pages.dev` → `https://whatadisaster.uk` + path/query/hash.
- **Preview/hashed deploys** (`<hash>.whatadisaster.pages.dev`) are deliberately **untouched** — they still show the dev-build warning, preserving the dev/preview workflow.

**Files changed:**
- `index.html` — split the existing `pages.dev` check: exact bare alias now `location.replace(...)` to `.uk`; other `*.pages.dev` hosts keep the dev-warning modal (`else if`).
- `how-it-works.html` — added the same bare-alias redirect at the top of its script (page previously had no hostname logic).

Path/query/hash preserved so deep links (e.g. `/how-it-works.html`) land on the matching `.uk` page. Not yet live — on branch `claude/site-usage-monitoring-sasyjf`; deploys when merged to `main`.
