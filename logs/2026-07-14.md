# 2026-07-14

## Credit line update: "designed and validated" + more prominence

Heather asked for the scenario content/terminology credit line to say she **designed and validated** it, not just reviewed it — a more accurate credit for her actual contribution. Also asked for it to be more visible than the original quiet dim-text footer (see `logs/2026-07-12.md` for the original addition).

**Change:** updated both credit line locations —
- `index.html:437` (bottom of `#results` screen)
- `how-it-works.html:162` (Overview tab)

Wording changed from "reviewed by" to "designed and validated by" in both. Style bumped in both: font-size up (`.8rem`/`.78rem` → `.9rem`), text colour brightened from `var(--dim)` to `var(--text)`/full opacity, and Heather's name bolded (`font-weight:600`) in the link. Kept the same locations, border-top/spacing treatment, and LinkedIn link — this was a copy + emphasis change, not a redesign or a new placement (splash screen was considered and explicitly ruled out, per user preference, to keep that screen focused on entering the exercise).
